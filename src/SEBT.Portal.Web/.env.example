# SEBT Portal Environment Configuration

# State Configuration (dc|co)
# Determines which design tokens are loaded and which theme is applied
STATE=dc

# Next.js automatically exposes NEXT_PUBLIC_* variables to the browser
# The STATE variable is converted to NEXT_PUBLIC_STATE in next.config.ts
